# Dry guitar audio assets

Drop browser-decodable dry DI guitar files here. The Phase 3 development server scans this
directory on every `/api/test-audio-files` request; no manifest or JavaScript edit is needed.
WAV is the deterministic reference format.
