import test from 'node:test';import assert from 'node:assert/strict';import {readFile} from 'node:fs/promises';
test('Cabinet WAM owns Core convolution in exactly one worklet node',async()=>{const p=await readFile('src/cabinet-wam/CabinetProcessor.js','utf8');const n=await readFile('src/cabinet-wam/CabinetNode.js','utf8');assert.match(p,/cabinet_process/);assert.doesNotMatch(p,/ConvolverNode|new AudioWorkletNode/);assert.match(n,/extends WamNode/);});
test('steady-state Cabinet processing body has no allocation or messaging',async()=>{const s=await readFile('src/cabinet-wam/CabinetProcessor.js','utf8');const body=s.slice(s.indexOf(' _process('),s.indexOf(' destroy()',s.indexOf(' _process(')));assert.doesNotMatch(body,/\bnew\s+|postMessage|Promise|console\.|JSON\.|setTimeout|setInterval/);});
test('production host graph chains NAM to Cabinet and destination',async()=>{const s=await readFile('examples/wam/main.js','utf8');assert.match(s,/rack\.output\.connect\(context\.destination\)/);});
